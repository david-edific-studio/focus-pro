{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions pour Focus Pro",
  "windows": ["main", "projection"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "dialog:allow-open",
    "fs:default",
    "fs:read-files",
    "fs:read-dirs",
    "shell:default"
  ]
}
